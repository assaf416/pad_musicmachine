<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:keyboards, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:keyboards, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :keyboard, url(:keyboards, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'keyboards/form', :locals => { :f => f } %>
 <% end %>
</div>
